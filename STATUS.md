- 2026-09-12 19:30 · Audience reframed (D-25/D-26): Progress Software + its ISV partner network; repeatable partner-accelerator programme; white-label by configuration. PMM and showroom leads redirected; all three product leads asked to add branding config, `GET /api/v1/branding`, white-label and build-your-own guides; platform v0.1.6 adds `readBranding`, shell branding, favicon. Internal-only research on the first intended pilot partner launched (not embedded in deliverables).
- 2026-09-12 20:15 · Platform v0.2.0: the generic application chrome all three product-experience passes built locally is now in the kit (left-rail `<arag-app-shell>`, page header, breadcrumb, tabs, filter bar, data table with sort/selection/bulk/pagination, drawer, typed confirm, empty state, stat strip, segmented, skeleton, snippet, timeline, split, tour, sign-in, menu, inline-SVG icon set). Fixed the four bugs the products worked around (`.arag a` vs `.arag-btn` specificity, button-only tabs, glyph-sized dropzone icon, shell-only `applyBranding`) and added an opt-in `App.static` SPA fallback. Official wordmarks ship in `ui/brand/` with the `#5ce500` usage rule. Template scaffolds a real workspace (list → record with tabs → settings → operator console in the same shell) with Playwright checks at 1440 and 390 px. Migration map per product is in CHANGELOG 0.2.0; the three product repos were not touched.
